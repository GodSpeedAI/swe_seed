title: AdaptationDecision
type: object
required:
  - decision_id
  - status
  - reason
properties:
  decision_id: { type: string }
  status: { type: string }
  reason: { type: string }
  proof_result: { type: string }
